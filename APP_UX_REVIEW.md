# 웹 → 앱 전환 UX 점검 (불편 예상 → 수정안 → 결과 화면)

현재 상태에서 "폰 앱"(설치형 PWA / Capacitor·TWA 래핑)으로 쓸 때 예상되는 불편을 우선순위로 정리.
이미 잘 된 것: PWA standalone, 상단 노치 여백, 결과 화면 분리, 정적 오프라인 캐시, hidden 모달 버그 수정.

---

## A. 핵심 — 이걸 고치면 "웹 같다"가 "앱 같다"로

### 1) 안드로이드 뒤로가기 / 스와이프 백
- **불편**: 결과 화면이나 지도·저장 시트가 열린 상태에서 시스템 뒤로가기를 누르면 모달이 닫히는 게 아니라 **앱이 그냥 종료**됨. 가장 거슬리는 부분.
- **수정**: 화면 전환·모달 열 때 History API(`pushState`)를 쌓고 `popstate`에서 닫기/뒤로 처리. (Capacitor면 `App.addListener('backButton')`)
- **결과 화면**: 뒤로가기 1번 → 열린 시트가 닫힘 → 결과 화면 → 설정 화면 순으로 자연스럽게 빠져나옴. 앱이 안 꺼짐.

### 2) 외부 링크가 앱 안에 갇힘
- **불편**: 길찾기·네이버 후기·블로그·지도 링크가 `_blank`라, WebView 앱에선 **앱 내부에 열려 뒤로가기도 안 되고 갇히거나** 흰 화면이 됨.
- **수정**: http(s) 외부 링크는 시스템 브라우저/카카오·네이버 앱으로 열도록 처리(Capacitor `Browser.open`, TWA는 기본 외부 처리).
- **결과 화면**: "길찾기" 누르면 **카카오맵 앱**이 뜨고, 끝나면 우리 앱으로 그대로 복귀.

### 3) 당겨서 새로고침에 상태 날아감
- **불편**: 결과를 보다 위로 당기면 pull-to-refresh로 **앱이 리로드 → 추천 결과·선택이 사라짐**.
- **수정**: `overscroll-behavior: contain`(이미 일부 적용 검토) + 스크롤 컨테이너 분리.
- **결과 화면**: 위로 당겨도 새로고침 안 되고, 결과가 유지됨.

### 4) 하단 탭 내비게이션이 없음
- **불편**: 앱인데 이동 수단이 헤더의 ♥ 하나뿐. 저장함/가본 곳을 오가기 번거로움.
- **수정**: 하단 고정 탭바 추가 — **[추천] [저장함] [가본곳/찜]**. (엄지 닿는 위치)
- **결과 화면**: 화면 아래 3개 탭. 탭만 누르면 화면 전환, 스크롤 없이 이동.

### 5) 장소 카드 액션이 너무 많고 작음
- **불편**: 장소마다 길찾기·네이버후기·전화·다녀옴·찜·바꾸기 칩이 줄줄이 → 작아서 **오터치**(권장 터치 44px 미달).
- **수정**: 자주 쓰는 1~2개(길찾기·찜)만 노출, 나머지는 **⋯ 더보기** 시트로. 버튼 키우기.
- **결과 화면**: 카드가 깔끔. 장소 누르면 사진 크게 + 큰 버튼(길찾기/전화/후기/바꾸기/찜)이 시트로.

---

## B. 권장 — 완성도/체감 품질

### 6) 로딩 체감(첫 추천 느림)
- **불편**: 무료 플랜 콜드스타트 + 라이브 검색으로 첫 추천이 수 초. 앱에선 "멈춘 것 같다"는 인상.
- **수정**: 스피너 대신 **스켈레톤 카드**(자리표시) + "지역 검색 중…" 진행문구, 결과 짧게 캐시.
- **결과 화면**: 누르는 즉시 회색 카드 3개가 깔리고 → 내용이 차오름. 빈 화면 없음.

### 7) 앱 재진입 시 상태 복원
- **불편**: 앱을 닫았다 열면 직전 추천/선택이 초기화.
- **수정**: 마지막 선택·결과를 localStorage에 저장 → 재실행 시 복원.
- **결과 화면**: 다시 열면 마지막 결과 화면(또는 마지막 지역 선택)이 그대로.

### 8) 오프라인 처리
- **불편**: 지하철 등에서 네트워크 끊기면 추천이 그냥 에러.
- **수정**: 오프라인 배너 + **저장한 코스·가본 곳·찜은 오프라인에서도 열람**(이미 localStorage).
- **결과 화면**: 상단에 "오프라인" 띠, 저장 탭은 정상 동작, 추천만 "연결되면 가능" 안내.

### 9) 아이콘 / 스플래시
- **불편**: 아이콘이 SVG 1종뿐 → 설치/스토어용 다양한 크기·스플래시 부족.
- **수정**: 192·512 PNG(+maskable) 아이콘과 스플래시 추가.
- **결과 화면**: 홈 화면 아이콘·실행 스플래시가 또렷하게.

---

## C. 상용화 시(스토어 출시) 추가

### 10) 로그인 화면(Basic Auth 대체)
- **불편**: 지금은 브라우저 기본 인증 팝업. 네이티브 앱에선 어색하고, 다중 사용자 불가.
- **수정**: 자체 로그인/세션 화면(또는 소셜 로그인)으로 교체.
- **결과 화면**: 첫 실행 시 우리 디자인의 로그인 화면 1회 → 이후 자동 로그인.

### 11) 이미지 저장/공유 네이티브화
- **불편**: WebView에서 `a.download`(이미지 저장)가 안 먹을 수 있음.
- **수정**: Capacitor Share/Filesystem 플러그인으로 갤러리 저장·네이티브 공유.
- **결과 화면**: "이미지" 누르면 **갤러리에 저장**되고 공유 시트가 바로 뜸.

### 12) 지도 임베드 키
- **불편**: `KAKAO_JS_KEY` 미설정이면 지도 대신 링크 목록(앱에선 지도가 핵심).
- **수정**: JS 키 등록 + 사용 도메인/패키지 등록.
- **결과 화면**: 코스 지도에 번호 마커 + 동선이 실제로 그려짐.

---

## 추천 진행 순서
1. **A1·A2·A3**(뒤로가기·외부링크·당겨서새로고침) — 체감 가장 큼, 코드도 적음.
2. **A4·A5**(하단 탭·카드 정리) — 앱다운 레이아웃.
3. **B6·B7**(스켈레톤·상태복원) — 완성도.
4. 그 다음 Capacitor 래핑 + **C** 항목으로 스토어 준비.
